import { Metadata } from "next";

export const metadata: Metadata = {
    title: `Assign Students to Class`,
};

export default function AssignStudentsPage() {
    return (
        <div>Assign Students Page</div>
    );
};