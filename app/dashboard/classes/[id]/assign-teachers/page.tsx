import { Metadata } from "next";

export const metadata: Metadata = {
    title: `Assign Teachers to Class`,
};

export default function AssignTeachersPage() {
    return (
        <div>Assign Teachers Page</div>
    );
};